import { notImplemented } from "./_stub.mjs";

export const id = "fukuchan";
const URL = "https://fuku-chan.info/gold/";

export const scrape = notImplemented(
  id,
  URL,
  "1gあたりの純度別価格表を公開しておらず、「買取相場を超えるプラス査定」を訴求する個別見積り制。自動取得できる数値が存在しない。"
);
